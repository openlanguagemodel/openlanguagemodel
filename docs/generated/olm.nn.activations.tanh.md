# `olm.nn.activations.tanh`

## Classes

### `Tanh(*, device=None, dtype=None) -> None`

Tanh activation wrapper.

#### Methods

- `forward(self, x: torch.Tensor) -> torch.Tensor`
  Apply activation to ``x``.
