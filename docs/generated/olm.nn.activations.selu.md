# `olm.nn.activations.selu`

## Classes

### `SELU(inplace: bool = False, *, device=None, dtype=None) -> None`

SELU activation wrapper.

#### Methods

- `forward(self, x: torch.Tensor) -> torch.Tensor`
  Apply activation to ``x``.
