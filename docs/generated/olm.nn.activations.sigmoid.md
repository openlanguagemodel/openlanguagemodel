# `olm.nn.activations.sigmoid`

## Classes

### `Sigmoid(*, device=None, dtype=None) -> None`

Sigmoid activation wrapper.

#### Methods

- `forward(self, x: torch.Tensor) -> torch.Tensor`
  Apply activation to ``x``.
