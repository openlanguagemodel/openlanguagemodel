# `olm.nn.activations.identity`

## Classes

### `Identity(*, device=None, dtype=None) -> None`

Identity activation wrapper.

#### Methods

- `forward(self, x: torch.Tensor) -> torch.Tensor`
  Apply activation to ``x``.
