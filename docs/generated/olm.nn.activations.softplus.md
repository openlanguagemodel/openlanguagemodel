# `olm.nn.activations.softplus`

## Classes

### `Softplus(beta: int = 1, threshold: int = 20, *, device=None, dtype=None) -> None`

Softplus activation wrapper.

#### Methods

- `forward(self, x: torch.Tensor) -> torch.Tensor`
  Apply activation to ``x``.
