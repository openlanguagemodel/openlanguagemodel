# `olm.nn.activations.softmax`

## Classes

### `Softmax(dim: int | None = -1, *, device=None, dtype=None) -> None`

Softmax activation wrapper.

#### Methods

- `forward(self, x: torch.Tensor) -> torch.Tensor`
  Apply activation to ``x``.
