# `olm.nn.activations.mish`

## Classes

### `Mish(inplace: bool = False, *, device=None, dtype=None) -> None`

Mish activation wrapper.

#### Methods

- `forward(self, x: torch.Tensor) -> torch.Tensor`
  Apply activation to ``x``.
